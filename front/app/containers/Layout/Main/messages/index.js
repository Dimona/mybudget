import { defineMessages } from 'react-intl';

export const scope = 'layout';

export default defineMessages({
    buttonLogout: {
        id: `${scope}.button.logout`,
        defaultMessage: `Logout`
    }
});
